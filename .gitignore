# Internal working doc (kept locally for session-to-session continuity,
# not pushed to the public repo per user instruction). PROGRESS.md and
# TODO.md are tracked and pushed -- they're the project's public dev log.
CLAUDE.md

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Data (gitignored per CLAUDE.md section 6 — datasets are downloaded, not committed)
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Databases (Module B state store output)
*.db
*.duckdb

# Pretrained weights (auto-downloaded by ultralytics on first run)
*.pt

# Vector store persistence (Module C)
.chroma/
*.index

# Reports build artifacts
reports/paper/*.aux
reports/paper/*.log
reports/paper/*.out
reports/paper/*.pdf

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Env
.env
